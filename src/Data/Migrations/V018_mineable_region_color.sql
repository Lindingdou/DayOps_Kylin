-- =============================================================================
-- V018: 可采区域加自定义 overlay 颜色列。
--
-- color = 用户为该区域指定的 overlay 颜色（6 位十六进制 RRGGBB，无 # 前缀）。
--         NULL = 按类别默认色（采场橙 / 外排蓝 / 内排青 / 可采绿）。
-- 见短期「确定可采区域」窗口的「颜色」分组（列表色块 + 改颜色）。
-- =============================================================================

ALTER TABLE mineable_region ADD COLUMN color TEXT;
