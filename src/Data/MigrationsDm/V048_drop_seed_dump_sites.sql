-- 由 build/sqlite2dm.py 从 V048_drop_seed_dump_sites.sql 自动生成, 请勿手改。
-- 要改请改源脚本或生成器, 然后重跑: python build/sqlite2dm.py

-- ─────────────────────────────────────────────────────────────────────────────
--  V048  删掉 V004 里那两条排土场种子（2026-08-20）
--
--  ══ 它们是什么 ══
--  `V004_seed_process_geometry.sql` 写死了两条：
--      ('D-N1', '北排土场', 'external', 12000, 8500, ... '外排土场,主排土,容量充足')
--      ('D-I1', '内排土场', 'internal',  5000, 3200, ... '内排土,减少运距,优先排土区')
--  12000 / 8500 / 5000 / 3200 全是脚本里编的数，与任何一个矿都没关系。
--
--  ══ 为什么必须删，而不是留着当兜底 ══
--  ① **名字对不上采掘单元台账**：台账里排土位置的场名是「内排土场1」，
--     而种子叫「内排土场」——绑定按名字精确配（ProcessZoneFaceSource），差一个字符就绑不上。
--     实测后果：17 个排土面**整月零入方**（日目标由入方推导，入方 0 就一条任务都排不出来），
--     而甘特上只是少几行，没有任何东西报错。
--  ② **库容差 40 倍**：真实台账 2112 个排土位置合计 1291.1 万m³；
--     种子写着 12000+5000 = 17000 万m³。库容闸比真实库容大 40 倍 = 等于没闸。
--  ③ **这个矿根本没有外排土场**：台账里 2112 个排土位置全属「内排土场1」，
--     一个外排的都没有。「北排土场」是凭空多出来的一个去向。
--
--  ══ 为什么不在这里补真数据 ══
--  真实排土场要从**采掘单元台账**（排土条带产出的位置清单）按场名分组重建 ——
--  那是文件不是库表，SQL 读不到。所以这里只负责**删干净**，
--  重建走「去向台账 → 按台账重建」那个按钮（它读同一份台账，名字因此天然对齐）。
--  **宁可空着也不留一份编的**：空着会报「一个去向都没有」，而编的那份看着完全正常。
--
--  ⚠ 只删这两条**特定 id**，不是清表 —— 人工建过的去向不许碰。
-- ─────────────────────────────────────────────────────────────────────────────

-- sink_profile 按 sink_id 挂在 dump_site 上（没有硬外键），一并清掉这两条的档案
DELETE FROM sink_profile WHERE sink_id IN ('D-N1', 'D-I1');

-- 盘点流水里属于这两条的记录一并清掉（它们记的是编出来的账的变动）
DELETE FROM sink_stocktake WHERE sink_id IN ('D-N1', 'D-I1');

DELETE FROM dump_site WHERE dump_id IN ('D-N1', 'D-I1');
